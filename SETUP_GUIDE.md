# دليل الإعداد والتطوير 🚀

هذا الدليل سيساعدك على إعداد تطبيق كوروكو وتشغيله بنجاح.

## المتطلبات الأساسية

### 1. تثبيت Android Studio
- اذهب إلى [Android Studio](https://developer.android.com/studio)
- حمّل وثبّت أحدث إصدار
- أثناء التثبيت، تأكد من تثبيت:
  - Android SDK
  - Android Emulator
  - Kotlin Plugin

### 2. تثبيت Java Development Kit (JDK)
```bash
# على macOS
brew install openjdk@17

# على Windows
# حمّل من https://www.oracle.com/java/technologies/downloads/

# على Linux
sudo apt-get install openjdk-17-jdk
```

### 3. الحصول على OpenAI API Key
1. اذهب إلى [OpenAI API Keys](https://platform.openai.com/api-keys)
2. سجل دخول أو أنشئ حساباً
3. انقر على "Create new secret key"
4. انسخ المفتاح (سيظهر مرة واحدة فقط!)
5. احفظه في مكان آمن

## خطوات الإعداد

### الخطوة 1: استنساخ المشروع
```bash
git clone <repository-url>
cd koroko_ai_companion
```

### الخطوة 2: إضافة مفتاح OpenAI API

**الطريقة الأولى: عبر gradle.properties**
```bash
# افتح ملف gradle.properties
nano gradle.properties

# استبدل:
OPENAI_API_KEY=YOUR_API_KEY_HERE

# بـ:
OPENAI_API_KEY=sk-your-actual-key-here
```

**الطريقة الثانية: عبر OpenAIService.kt**
```kotlin
// افتح: app/src/main/kotlin/com/example/korokoai/network/OpenAIService.kt
// استبدل:
private val apiKey = "YOUR_OPENAI_API_KEY"

// بـ:
private val apiKey = "sk-your-actual-key-here"
```

### الخطوة 3: فتح المشروع في Android Studio
1. افتح Android Studio
2. اختر "Open" أو "File > Open"
3. انتقل إلى مجلد `koroko_ai_companion`
4. اختر المجلد وانقر "OK"
5. انتظر حتى ينتهي Gradle من المزامنة

### الخطوة 4: تشغيل التطبيق

**على محاكي (Emulator):**
1. في Android Studio، انقر على "AVD Manager" (أيقونة الهاتف)
2. أنشئ جهازاً افتراضياً جديداً إذا لم يكن لديك واحد
3. شغّل الجهاز الافتراضي
4. انقر على "Run" أو اضغط Shift+F10

**على جهاز حقيقي:**
1. وصّل جهازك بـ USB
2. فعّل "Developer Mode" على الجهاز
3. انقر على "Run" في Android Studio

## استكشاف الأخطاء

### المشكلة: "Build failed"
**الحل:**
```bash
# نظّف المشروع
./gradlew clean

# أعد البناء
./gradlew build
```

### المشكلة: "API Key not found"
- تأكد من أنك أضفت المفتاح بشكل صحيح
- تحقق من عدم وجود مسافات إضافية
- أعد تشغيل Android Studio

### المشكلة: "Gradle sync failed"
```bash
# حدّث Gradle
./gradlew wrapper --gradle-version latest

# أعد المزامنة
./gradlew sync
```

### المشكلة: "Emulator not starting"
- تأكد من تثبيت Android SDK بشكل صحيح
- جرّب إنشاء جهاز افتراضي جديد
- تحقق من أن جهازك يدعم التصور الافتراضي

## الميزات الحالية

✅ محادثات نصية مع كوروكو  
✅ واجهة مستخدم جميلة بـ Jetpack Compose  
✅ دعم اللغة العربية  
✅ تكامل مع OpenAI API  
✅ ذاكرة محادثات بسيطة  

## الميزات المخطط لها

🔜 صور متحركة لكوروكو  
🔜 حفظ المحادثات في قاعدة بيانات  
🔜 محادثات صوتية  
🔜 ألعاب وأنشطة تفاعلية  
🔜 تحسينات الأداء  

## نصائح التطوير

### تصحيح الأخطاء (Debugging)
1. انقر على "Debug" بدلاً من "Run"
2. استخدم "Logcat" لعرض السجلات
3. ضع نقاط توقف (Breakpoints) بالنقر على رقم السطر

### تحسين الأداء
- استخدم "Profiler" لمراقبة الذاكرة والمعالج
- تجنب العمليات الثقيلة على الـ Main Thread
- استخدم Coroutines للعمليات غير المتزامنة

### اختبار الميزات
- استخدم جهازاً افتراضياً بأحجام شاشات مختلفة
- اختبر على أجهزة حقيقية إذا أمكن
- تحقق من الاستجابة على الاتصالات البطيئة

## الموارد المفيدة

- [Android Developers](https://developer.android.com/)
- [Jetpack Compose Documentation](https://developer.android.com/jetpack/compose)
- [Kotlin Documentation](https://kotlinlang.org/docs/)
- [OpenAI API Documentation](https://platform.openai.com/docs/)

## الدعم والمساعدة

إذا واجهت مشاكل:
1. تحقق من الأخطاء في Logcat
2. ابحث عن الخطأ على Stack Overflow
3. راجع التوثيق الرسمية
4. افتح issue في المشروع

---

**استمتع بتطوير تطبيقك! 💙**
