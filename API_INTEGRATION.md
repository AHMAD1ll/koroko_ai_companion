# دليل تكامل OpenAI API 🔑

هذا الدليل يشرح كيفية تكامل تطبيق كوروكو مع OpenAI API بشكل صحيح وآمن.

## الخطوة 1: الحصول على OpenAI API Key

### 1.1 إنشاء حساب OpenAI
1. اذهب إلى [OpenAI Platform](https://platform.openai.com/)
2. انقر على "Sign up" أو "Log in"
3. أكمل عملية التسجيل أو الدخول

### 1.2 إنشاء مفتاح API
1. اذهب إلى [API Keys](https://platform.openai.com/api-keys)
2. انقر على "Create new secret key"
3. اختر اسماً للمفتاح (مثل: "Koroko Android App")
4. انقر على "Create secret key"
5. **انسخ المفتاح فوراً** (سيظهر مرة واحدة فقط!)
6. احفظه في مكان آمن

### 1.3 إعداد الفواتير (Billing)
1. اذهب إلى [Billing Overview](https://platform.openai.com/account/billing/overview)
2. أضف طريقة دفع (بطاقة ائتمان)
3. تحقق من الحد الأقصى للنفقات الشهرية

## الخطوة 2: إضافة المفتاح إلى التطبيق

### الطريقة الأولى: عبر OpenAIService.kt (الطريقة البسيطة)

1. افتح الملف:
```
app/src/main/kotlin/com/example/korokoai/network/OpenAIService.kt
```

2. ابحث عن هذا السطر:
```kotlin
private val apiKey = "YOUR_OPENAI_API_KEY"
```

3. استبدله بمفتاحك الفعلي:
```kotlin
private val apiKey = "sk-proj-xxxxxxxxxxxxxxxxxxxxxxxxxx"
```

### الطريقة الثانية: عبر Environment Variables (الطريقة الآمنة)

**ملاحظة:** هذه الطريقة أفضل للإنتاج.

1. افتح `gradle.properties`:
```
OPENAI_API_KEY=sk-proj-xxxxxxxxxxxxxxxxxxxxxxxxxx
```

2. في `OpenAIService.kt`:
```kotlin
private val apiKey = System.getenv("OPENAI_API_KEY") ?: "YOUR_OPENAI_API_KEY"
```

## الخطوة 3: اختبار التكامل

### اختبار بسيط
1. شغّل التطبيق
2. اكتب رسالة بسيطة مثل: "مرحباً"
3. تحقق من أن كوروكو ترد بشكل طبيعي

### التحقق من الأخطاء
إذا لم تحصل على رد:
1. افتح Logcat في Android Studio
2. ابحث عن أخطاء تتعلق بـ API
3. تحقق من:
   - صحة المفتاح
   - الاتصال بالإنترنت
   - الحد الأقصى للنفقات

## الخطوة 4: مراقبة الاستخدام

### عرض الاستخدام
1. اذهب إلى [Usage](https://platform.openai.com/account/usage/overview)
2. اختر الفترة الزمنية
3. اعرض تفاصيل الاستخدام

### تحديد الحد الأقصى للنفقات
1. اذهب إلى [Billing Settings](https://platform.openai.com/account/billing/limits)
2. اضبط "Hard limit" و "Soft limit"
3. سيتم إيقاف الخدمة عند الوصول للحد

## الخطوة 5: تحسين التكاليف

### استخدام نماذج أقل تكلفة
```kotlin
// بدلاً من:
addProperty("model", "gpt-4")

// استخدم:
addProperty("model", "gpt-4-mini")  // أرخص بـ 10 مرات
```

### تقليل عدد الرسائل المحفوظة
```kotlin
// بدلاً من:
val recentHistory = conversationHistory.takeLast(20)

// استخدم:
val recentHistory = conversationHistory.takeLast(10)
```

### تقليل max_tokens
```kotlin
// بدلاً من:
addProperty("max_tokens", 1000)

// استخدم:
addProperty("max_tokens", 600)
```

## الأسعار الحالية (تقريبية)

| النموذج | السعر (لكل 1000 token) |
| :--- | :--- |
| gpt-4 | $0.03 (input) / $0.06 (output) |
| gpt-4-turbo | $0.01 (input) / $0.03 (output) |
| gpt-4-mini | $0.00015 (input) / $0.0006 (output) |
| gpt-3.5-turbo | $0.0005 (input) / $0.0015 (output) |

**ملاحظة:** الأسعار قد تتغير. تحقق من [OpenAI Pricing](https://openai.com/pricing/)

## استكشاف الأخطاء الشائعة

### ❌ خطأ: "Invalid API Key"
**الحل:**
- تحقق من صحة نسخ المفتاح
- تأكد من عدم وجود مسافات إضافية
- جرّب إنشاء مفتاح جديد

### ❌ خطأ: "Insufficient quota"
**الحل:**
- أضف طريقة دفع
- تحقق من الحد الأقصى للنفقات
- جرّب استخدام نموذج أرخص

### ❌ خطأ: "Rate limit exceeded"
**الحل:**
- انتظر بعض الوقت
- قلل عدد الرسائل المرسلة
- ارفع حد النفقات

### ❌ خطأ: "Network error"
**الحل:**
- تحقق من الاتصال بالإنترنت
- جرّب إعادة تشغيل التطبيق
- تحقق من جدار الحماية

## أفضل الممارسات الأمنية

### ✅ افعل:
- احفظ المفتاح في متغيرات البيئة
- استخدم HTTPS دائماً
- قيّد الوصول إلى المفتاح
- راقب الاستخدام بانتظام

### ❌ لا تفعل:
- لا تضع المفتاح في كود مصدري
- لا تشارك المفتاح مع أحد
- لا تستخدم نفس المفتاح لعدة تطبيقات
- لا تترك المفتاح في ملفات النسخ الاحتياطية

## الدعم والمساعدة

- [OpenAI Documentation](https://platform.openai.com/docs/)
- [OpenAI Community](https://community.openai.com/)
- [API Status](https://status.openai.com/)

---

**تم! الآن تطبيقك جاهز للعمل مع OpenAI API! 🚀**
